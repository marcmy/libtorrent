name: Selective recheck stale-handle staging

on:
  issue_comment:
    types: [created]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.issue.pull_request && github.event.issue.number == 33 && github.event.comment.body == '/apply-selective-release-files'
    runs-on: ubuntu-latest
    steps:
      - name: Check out helper from default branch
        uses: actions/checkout@v4
        with:
          ref: custom/qbittorrent-2.1
          path: helper

      - name: Check out hotfix branch
        uses: actions/checkout@v4
        with:
          ref: fix/selective-recheck-crash
          fetch-depth: 0
          path: work

      - name: Apply stale-handle fix and regression coverage
        working-directory: work
        run: python ../helper/.github/selective_recheck_release_files.py

      - name: Commit source changes
        working-directory: work
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/torrent_handle.cpp test/test_selective_recheck.cpp
          git diff --cached --check
          git commit -m "fix: reopen files before selective recheck"
          git push origin HEAD:fix/selective-recheck-crash
