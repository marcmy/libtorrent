name: Linux (disabled)

on: workflow_dispatch

permissions:
  contents: read

jobs:
  disabled:
    name: Ubuntu CI disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain platform scope
        run: echo "Generic Ubuntu CI is disabled for this Windows-focused fork. Future Linux support should target Arch Linux."
