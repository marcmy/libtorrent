name: "CodeQL config"

paths-ignore:
  - deps/**

query-filters:
  - exclude:
      id: actions/missing-workflow-permissions
  - exclude:
      id: actions/unpinned-tag
  - exclude:
      id: py/file-not-closed
